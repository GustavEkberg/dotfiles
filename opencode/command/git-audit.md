---
description: Audit an unfamiliar codebase via git history and write docs/CODEBASE_AUDIT.md
---

Run a git-history-driven audit of the current repo and write the result to `docs/CODEBASE_AUDIT.md`.

Load the skill:

```
skill({ name: 'git-audit' })
```

Then follow its workflow.

<user-request>
$ARGUMENTS
</user-request>
