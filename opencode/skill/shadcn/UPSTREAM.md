# shadcn skill provenance

Vendored from `https://github.com/shadcn-ui/ui`, path `skills/shadcn`.

Official global install command, if not using the vendored dotfiles copy:

```sh
pnpm dlx skills add shadcn/ui --global --yes
```

Refresh with the official source, then keep only files used by OpenCode:

- `SKILL.md`
- `cli.md`
- `customization.md`
- `mcp.md`
- `registry.md`
- `rules/*.md`
